package data

import java.net.URI
import java.time.LocalDateTime

import kortglad.data.{CardAvarages, CardStat, MatchStat, RefereeStats}
import io.circe._
import io.circe.generic.auto._
import io.circe.jawn._
import io.circe.syntax._


/**
  *
  */
object DataTests extends App{

//  private val stat = new CardStat(1,2,3)
//  println(stat.asJson)
//
//  println(CardAvarages.from(stat, 1).asJson)
//
//  private val stats = new RefereeStats(List(MatchStat(URI.create("http://www.vg.no"), LocalDateTime.now(), "home","away", stat)))
//  println(stats.asJson)
//


}
